# LeLamp v2.0 测试清单

**测试版本**: v2.0
**测试日期**: _________
**测试人员**: _________
**硬件设备**: Raspberry Pi 4B / 开发机

---

## 📋 测试环境准备

### 1. 环境检查

- [ ] Python 3.12+ 已安装
  ```bash
  python3 --version  # 应显示 3.12+
  ```

- [ ] UV Package Manager 已安装
  ```bash
  uv --version
  ```

- [ ] 依赖包已安装
  ```bash
  # Raspberry Pi
  uv sync --extra hardware

  # 开发机
  uv sync
  ```

- [ ] .env 文件已配置
  ```bash
  ls -la .env  # 文件存在
  grep "LIVEKIT_URL" .env  # 配置已填写
  ```

### 2. 硬件连接检查 (仅 Raspberry Pi)

- [ ] 电机串口连接
  ```bash
  ls -l /dev/ttyACM0  # 设备存在
  ```

- [ ] 摄像头连接
  ```bash
  ls -l /dev/video0  # 设备存在
  ```

- [ ] LED 灯带供电
  - GPIO 12 连接正常
  - 5V 电源接通

- [ ] 音频设备
  ```bash
  arecord -l  # 麦克风存在
  aplay -l    # 扬声器存在
  ```

---

## 🚀 启动测试

### 3. LeLamp Agent 启动

- [ ] 启动成功
  ```bash
  cd ~/lelamp_runtime
  sudo uv run main.py console
  ```

- [ ] 日志输出正常
  ```
  预期输出:
  ✅ INFO:root:config ready: lamp_id=lelamp port=/dev/ttyACM0
  ✅ INFO:root:MotorsService started
  ✅ INFO:root:RGBService started
  ✅ INFO:root:VisionService started
  ✅ INFO:livekit:Connected to LiveKit
  ```

- [ ] 服务状态正常
  - MotorsService: RUNNING
  - RGBService: RUNNING
  - VisionService: RUNNING

- [ ] 无错误日志
  - 无 ERROR 级别日志
  - 无 CRITICAL 级别日志

### 4. Token 生成

- [ ] Token 生成成功
  ```bash
  uv run python scripts/generate_client_token.py --room lelamp-room --user test
  ```

- [ ] Token 格式正确
  ```
  预期输出:
  Room: lelamp-room
  User: test
  --------------------
  Token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
  --------------------
  ```

- [ ] Token 复制到剪贴板
  - 完整 Token 字符串
  - 无多余空格或换行

### 5. Web Client 启动

- [ ] HTTP 服务器启动
  ```bash
  python3 -m http.server 8000
  ```

- [ ] 浏览器访问成功
  - URL: http://localhost:5173
  - 页面加载完整

- [ ] UI 显示正常
  - 连接面板显示
  - 4 个 Tab 可见
  - 无 JavaScript 错误

---

## 🔌 连接测试

### 6. LiveKit 连接

- [ ] 填写 Server URL
  - 格式: `wss://your-project.livekit.cloud`
  - 无多余空格

- [ ] 填写 Token
  - 完整 Token 字符串
  - 无多余空格

- [ ] 点击"连接设备"按钮
  - 按钮变为"连接中..."
  - Toast 通知显示

- [ ] 连接成功
  ```
  预期结果:
  ✅ Toast 显示: "✅ 连接成功"
  ✅ 按钮变为"断开连接"
  ✅ 视频流显示
  ✅ 控制面板激活
  ```

### 7. 视频流测试

- [ ] 视频显示正常
  - 画面清晰
  - 无黑屏
  - 无卡顿

- [ ] 分辨率正确
  - 1024x768 (默认配置)
  - 画面比例正确

- [ ] 帧率流畅
  - 目测 30fps
  - 无明显延迟

---

## 🎙️ 语音对话测试

### 8. 语音识别 (STT)

- [ ] 麦克风权限已授予
  - 浏览器提示已允许
  - 绿色麦克风图标

- [ ] 说话触发识别
  ```
  测试语句: "你好"
  预期:
  ✅ LED 变为蓝色 (Listening)
  ✅ 聊天区显示识别结果
  ```

- [ ] 识别准确率测试
  | 测试语句 | 识别结果 | 正确性 |
  |---------|---------|--------|
  | "你好" | _______ | ☐ 正确 / ☐ 错误 |
  | "现在几点了" | _______ | ☐ 正确 / ☐ 错误 |
  | "讲个笑话" | _______ | ☐ 正确 / ☐ 错误 |
  | "帮我检查作业" | _______ | ☐ 正确 / ☐ 错误 |

### 9. 语音合成 (TTS)

- [ ] 回复语音播放
  ```
  测试: 说"你好"
  预期:
  ✅ LED 变为彩色 (Speaking)
  ✅ 扬声器播放语音
  ✅ 聊天区显示回复文字
  ```

- [ ] 音质清晰
  - 无杂音
  - 无失真
  - 音量适中

- [ ] 发音自然
  - 语调自然
  - 语速适中
  - 停顿合理

### 10. 对话状态指示

- [ ] 空闲状态 (Idle)
  ```
  条件: 无对话进行
  预期: LED 显示暖白色 (255, 244, 229)
  ```

- [ ] 倾听状态 (Listening)
  ```
  条件: 正在说话
  预期: LED 显示蓝色 (0, 140, 255)
  ```

- [ ] 思考状态 (Thinking)
  ```
  条件: 等待回复
  预期: LED 显示紫色 (180, 0, 255)
  ```

- [ ] 说话状态 (Speaking)
  ```
  条件: 播放语音
  预期: LED 显示随机彩色 + 呼吸效果
  ```

---

## 🔍 视觉功能测试

### 11. 拍照识别

- [ ] 按钮点击
  - 点击"🔍 拍照识别"按钮
  - 按钮变为"识别中..."

- [ ] 隐私 LED 指示
  ```
  预期:
  ✅ LED 变为红色
  ✅ 隐私仪表盘显示"🔴 摄像头激活"
  ```

- [ ] 识别结果返回
  ```
  测试: 举起一个苹果
  预期:
  ✅ Toast 显示"✅ 识别完成"
  ✅ 弹窗显示识别结果
  ✅ 显示拍摄的图片
  ✅ 描述准确
  ```

- [ ] 多物体识别
  | 测试物体 | 识别结果 | 准确性 |
  |---------|---------|--------|
  | 苹果 | _______ | ☐ 准确 / ☐ 不准确 |
  | 书本 | _______ | ☐ 准确 / ☐ 不准确 |
  | 水杯 | _______ | ☐ 准确 / ☐ 不准确 |
  | 人脸 | _______ | ☐ 准确 / ☐ 不准确 |

### 12. 检查作业

- [ ] 准备测试作业
  - 手写数学题 (加减乘除)
  - 纸张平放
  - 光线充足

- [ ] 按钮点击
  - 点击"📚 检查作业"按钮
  - 按钮变为"检查中..."

- [ ] 识别结果准确性
  ```
  测试题目:
  1. 5 + 3 = 8
  2. 12 - 7 = 5
  3. 6 × 4 = 24
  4. 18 ÷ 3 = 6

  识别结果:
  ☐ 题1: _________ (正确/错误)
  ☐ 题2: _________ (正确/错误)
  ☐ 题3: _________ (正确/错误)
  ☐ 题4: _________ (正确/错误)

  正确率: _________
  ```

### 13. 推送飞书

⚠️ **前置条件**: 需要配置飞书 API

- [ ] 环境变量已配置
  ```bash
  grep "FEISHU_APP_ID" .env  # 已配置
  grep "FEISHU_APP_SECRET" .env  # 已配置
  grep "FEISHU_RECEIVE_ID" .env  # 已配置
  ```

- [ ] 按钮点击
  - 点击"✈️ 推送飞书"按钮
  - Toast 显示"发送中..."

- [ ] 飞书接收成功
  ```
  预期:
  ✅ Toast 显示"✅ 推送成功"
  ✅ 飞书群收到消息
  ✅ 消息包含图片
  ✅ 消息包含时间戳
  ```

---

## 🎭 动作表情测试

### 14. 预设动作测试

**测试方法**: 点击 Web Client 中的动作按钮

- [ ] 点头 (nod)
  ```
  预期:
  ✅ 电机上下摆动
  ✅ 动作流畅
  ✅ 无异响
  ✅ 持续时间: ~6秒
  ```

- [ ] 摇头 (shake) ⭐ **新增**
  ```
  预期:
  ✅ 电机左右摆动
  ✅ 动作流畅
  ✅ 无异响
  ✅ 持续时间: ~1秒
  ```

- [ ] 兴奋 (excited)
  ```
  预期:
  ✅ 快速摆动
  ✅ 动作幅度大
  ✅ 无异响
  ✅ 持续时间: ~15秒
  ```

- [ ] 睡觉 (sleep) ⭐ **新增**
  ```
  预期:
  ✅ 缓慢低头
  ✅ 动作平滑
  ✅ 无异响
  ✅ 持续时间: ~1秒
  ```

- [ ] 跳舞 (dance) ⭐ **新增**
  ```
  预期:
  ✅ 有节奏摆动
  ✅ 多关节协调
  ✅ 无异响
  ✅ 持续时间: ~0.8秒
  ```

- [ ] 思考 (think) ⭐ **新增**
  ```
  预期:
  ✅ 缓慢转动
  ✅ 动作平滑
  ✅ 无异响
  ✅ 持续时间: ~0.9秒
  ```

### 15. 动作冷却测试

- [ ] 冷却机制生效
  ```
  测试步骤:
  1. 点击"点头"按钮
  2. 立即再次点击"点头"按钮

  预期:
  ✅ 第一次执行正常
  ✅ 第二次被冷却阻止
  ✅ Toast 显示"动作执行太频繁"
  ```

- [ ] 冷却时间正确
  ```
  默认冷却时间: 2 秒 (LELAMP_MOTION_COOLDOWN_S)

  测试:
  ☐ 点击动作按钮
  ☐ 等待 2 秒
  ☐ 再次点击
  ☐ 第二次执行成功
  ```

### 16. 语音触发动作

- [ ] 语音指令识别
  | 测试指令 | 执行动作 | 成功 |
  |---------|---------|------|
  | "点个头" | nod | ☐ |
  | "摇摇头" | shake | ☐ |
  | "跳个舞" | dance | ☐ |
  | "你真棒" | excited (自动) | ☐ |

---

## 💡 灯光控制测试

### 17. 纯色灯光

- [ ] 调色盘功能
  ```
  测试步骤:
  1. 切换到"💡 灯光魔法" Tab
  2. 点击颜色选择器
  3. 选择红色 (RGB: 255, 0, 0)
  4. 点击"设置"按钮

  预期:
  ✅ LED 显示红色
  ✅ Toast 显示"✅ 灯光设置成功"
  ```

- [ ] 快速颜色按钮
  | 颜色 | RGB 值 | LED 显示 |
  |------|--------|---------|
  | 🔴 暖红 | (220, 60, 60) | ☐ 正确 |
  | 💖 粉红 | (255, 105, 180) | ☐ 正确 |
  | 🟠 橙色 | (255, 140, 0) | ☐ 正确 |
  | 🟡 金黄 | (255, 215, 0) | ☐ 正确 |
  | 🟢 浅绿 | (144, 238, 144) | ☐ 正确 |
  | 🔵 天蓝 | (135, 206, 250) | ☐ 正确 |
  | 🟣 紫色 | (138, 43, 226) | ☐ 正确 |
  | ⚪ 暖白 | (255, 244, 229) | ☐ 正确 |

- [ ] 关灯功能
  ```
  测试:
  1. 点击"⚫ 关灯"按钮

  预期:
  ✅ LED 熄灭 (全黑)
  ✅ Toast 显示"✅ 已关灯"
  ```

### 18. 灯效动画

- [ ] 💗 呼吸灯 (breathing)
  ```
  预期:
  ✅ 灯光缓慢呼吸
  ✅ 亮度渐变平滑
  ✅ 周期约 3-5 秒
  ✅ 可持续运行
  ```

- [ ] 🌈 彩虹 (rainbow)
  ```
  预期:
  ✅ 颜色渐变流动
  ✅ 色彩过渡平滑
  ✅ 周期约 5-10 秒
  ✅ 颜色顺序正确
  ```

- [ ] 🌊 波浪 (wave)
  ```
  预期:
  ✅ 波浪起伏效果
  ✅ 动画流畅
  ✅ 方向明确
  ✅ 周期约 3-5 秒
  ```

- [ ] 🔥 火焰 (fire)
  ```
  预期:
  ✅ 火焰跳动效果
  ✅ 颜色随机变化
  ✅ 暖色调为主
  ✅ 自然逼真
  ```

- [ ] 🎆 烟花 (fireworks)
  ```
  预期:
  ✅ 烟花绽放效果
  ✅ 多个爆炸点
  ✅ 轨迹清晰
  ✅ 色彩鲜艳
  ```

- [ ] ⭐ 星空 (starry)
  ```
  预期:
  ✅ 星星闪烁效果
  ✅ 随机位置
  ✅ 亮度变化
  ✅ 梦幻氛围
  ```

- [ ] 停止灯效
  ```
  测试:
  1. 启动任意灯效
  2. 点击"⏹ 停止灯效"按钮

  预期:
  ✅ 灯效立即停止
  ✅ LED 保持当前颜色
  ✅ Toast 显示"✅ 灯效已停止"
  ```

### 19. 灯光覆盖机制

- [ ] 手动灯光覆盖对话状态
  ```
  测试步骤:
  1. 设置红色灯光
  2. 立即说话触发对话

  预期:
  ✅ LED 保持红色 (不变为蓝色)
  ✅ 覆盖持续 10 秒 (默认 LELAMP_LIGHT_OVERRIDE_S)
  ✅ 10 秒后恢复对话状态指示
  ```

- [ ] 动作抑制机制
  ```
  测试步骤:
  1. 设置红色灯光
  2. 立即点击"点头"按钮

  预期:
  ✅ 动作被抑制 2 秒 (默认 LELAMP_SUPPRESS_MOTION_AFTER_LIGHT_S)
  ✅ Toast 显示"刚设置了灯光,稍等再动"
  ```

---

## 🌐 联网搜索测试

⚠️ **前置条件**: 需要配置博查 API

### 20. 搜索功能

- [ ] 环境变量已配置
  ```bash
  grep "BOCHA_API_KEY" .env  # 已配置
  ```

- [ ] 实时信息查询
  | 测试问题 | 搜索结果 | 准确性 |
  |---------|---------|--------|
  | "今天天气怎么样？" | _______ | ☐ 准确 / ☐ 不准确 |
  | "LeLamp 是什么？" | _______ | ☐ 准确 / ☐ 不准确 |
  | "北京时间" | _______ | ☐ 准确 / ☐ 不准确 |

- [ ] 速率限制测试
  ```
  测试: 连续发送 5 个搜索请求

  预期:
  ✅ 前 2 个请求成功
  ✅ 后 3 个请求被限流
  ✅ Toast 显示"请求太频繁"
  ```

---

## 🔧 系统管理测试

### 21. 音量控制

- [ ] 语音控制音量
  ```
  测试指令: "把音量调到 80"

  预期:
  ✅ 系统音量变为 80%
  ✅ 回复确认消息
  ```

- [ ] 音量范围验证
  | 目标音量 | 系统音量 | 成功 |
  |---------|---------|------|
  | 0% | _______ | ☐ |
  | 50% | _______ | ☐ |
  | 100% | _______ | ☐ |

### 22. OTA 更新

⚠️ **前置条件**: 需要配置 OTA 服务器

- [ ] 环境变量已配置
  ```bash
  grep "LELAMP_OTA_URL" .env  # 已配置
  ```

- [ ] 检查更新
  ```
  测试指令: "检查更新"

  预期:
  ✅ 查询 OTA 服务器
  ✅ 显示当前版本
  ✅ 显示最新版本
  ✅ 显示更新内容
  ```

- [ ] 执行更新
  ```
  测试指令: "确认更新"

  预期:
  ✅ 下载更新文件
  ✅ 验证 SHA256 哈希
  ✅ 安装更新
  ✅ 服务重启
  ```

---

## 🛡️ 隐私保护测试

### 23. 摄像头隐私指示

- [ ] LED 指示灯
  ```
  测试步骤:
  1. 点击"拍照识别"按钮

  预期:
  ✅ LED 立即变为红色
  ✅ 拍照完成后恢复原状态
  ```

- [ ] 状态文字更新
  ```
  测试步骤:
  1. 点击"拍照识别"按钮
  2. 观察隐私仪表盘

  预期:
  ✅ 显示"🔴 摄像头激活"
  ✅ 拍照完成后显示"⚪ 摄像头空闲"
  ```

### 24. 用户同意机制

- [ ] 首次拍照请求同意
  ```
  测试: 重启 Agent 后首次拍照

  预期:
  ✅ LED 闪烁黄色
  ✅ 等待用户同意
  ✅ (实际需要语音/按钮确认)
  ```

- [ ] 同意有效期
  ```
  默认 TTL: 1 小时

  测试:
  ☐ 同意后 1 小时内拍照无需再次同意
  ☐ 1 小时后拍照需要重新同意
  ```

---

## 🔒 安全测试

### 25. 设备授权

⚠️ **生产环境必需**

- [ ] License Key 验证
  ```bash
  # 测试无效 License Key
  LELAMP_LICENSE_KEY=invalid_key

  预期:
  ✅ Agent 启动失败
  ✅ 日志显示"授权验证失败"
  ```

- [ ] 开发模式跳过
  ```bash
  # 测试开发模式
  LELAMP_DEV_MODE=1

  预期:
  ✅ Agent 启动成功
  ✅ 日志显示"开发模式: 跳过授权"
  ```

### 26. API 安全

- [ ] 环境变量隔离
  ```bash
  # 检查 .env 未提交到 Git
  git status | grep ".env"

  预期:
  ✅ .env 不在跟踪列表
  ✅ .gitignore 包含 .env
  ```

- [ ] HTTPS 强制
  ```
  测试: 尝试使用 http:// 的 API

  预期:
  ✅ 请求被拒绝
  ✅ 日志显示"仅支持 HTTPS"
  ```

---

## 📊 性能测试

### 27. 延迟测试

- [ ] 视频延迟
  ```
  测试方法: 手在摄像头前挥动,观察画面延迟

  测试结果:
  延迟: _______ ms
  预期: < 200ms
  ☐ 通过 / ☐ 未通过
  ```

- [ ] 语音识别延迟
  ```
  测试方法: 说话到识别结果显示

  测试结果:
  延迟: _______ ms
  预期: < 500ms
  ☐ 通过 / ☐ 未通过
  ```

- [ ] 视觉识别延迟
  ```
  测试方法: 点击按钮到结果返回

  测试结果:
  延迟: _______ 秒
  预期: 3-8 秒
  ☐ 通过 / ☐ 未通过
  ```

- [ ] 动作响应延迟
  ```
  测试方法: 点击按钮到电机开始移动

  测试结果:
  延迟: _______ ms
  预期: < 100ms
  ☐ 通过 / ☐ 未通过
  ```

- [ ] 灯光响应延迟
  ```
  测试方法: 点击按钮到 LED 改变颜色

  测试结果:
  延迟: _______ ms
  预期: < 50ms
  ☐ 通过 / ☐ 未通过
  ```

### 28. 资源占用

- [ ] CPU 占用率
  ```bash
  top -p $(pgrep -f main.py)

  测试结果:
  CPU: _______ %
  预期: 30-50%
  ☐ 通过 / ☐ 未通过
  ```

- [ ] 内存占用
  ```bash
  ps aux | grep main.py

  测试结果:
  Memory: _______ MB
  预期: 800MB - 1.2GB
  ☐ 通过 / ☐ 未通过
  ```

---

## 🔁 压力测试

### 29. 长时间运行

- [ ] 24 小时稳定性
  ```
  测试步骤:
  1. 启动 Agent
  2. 运行 24 小时
  3. 观察系统状态

  测试结果:
  运行时长: _______ 小时
  崩溃次数: _______ 次
  ☐ 通过 (0 次崩溃) / ☐ 未通过
  ```

### 30. 高频操作

- [ ] 连续动作指令
  ```
  测试: 在冷却期外连续点击 10 次动作按钮

  测试结果:
  成功次数: _______ / 10
  错误次数: _______ 次
  ☐ 通过 / ☐ 未通过
  ```

- [ ] 连续视觉识别
  ```
  测试: 连续点击 5 次"拍照识别"

  测试结果:
  成功次数: _______ / 5
  错误次数: _______ 次
  ☐ 通过 / ☐ 未通过
  ```

---

## 🐛 已知问题记录

### 问题 1
**描述**: _________________________
**复现步骤**:
1. _________________________
2. _________________________
3. _________________________

**预期行为**: _________________________
**实际行为**: _________________________
**严重程度**: ☐ 低 / ☐ 中 / ☐ 高 / ☐ 紧急
**状态**: ☐ 待修复 / ☐ 修复中 / ☐ 已修复

---

### 问题 2
**描述**: _________________________
**复现步骤**:
1. _________________________
2. _________________________
3. _________________________

**预期行为**: _________________________
**实际行为**: _________________________
**严重程度**: ☐ 低 / ☐ 中 / ☐ 高 / ☐ 紧急
**状态**: ☐ 待修复 / ☐ 修复中 / ☐ 已修复

---

### 问题 3
**描述**: _________________________
**复现步骤**:
1. _________________________
2. _________________________
3. _________________________

**预期行为**: _________________________
**实际行为**: _________________________
**严重程度**: ☐ 低 / ☐ 中 / ☐ 高 / ☐ 紧急
**状态**: ☐ 待修复 / ☐ 修复中 / ☐ 已修复

---

## ✅ 测试总结

### 功能完整度

| 模块 | 测试项 | 通过 | 失败 | 完成率 |
|------|--------|------|------|--------|
| 环境准备 | 5 | ☐ | ☐ | __% |
| 启动测试 | 3 | ☐ | ☐ | __% |
| 连接测试 | 2 | ☐ | ☐ | __% |
| 语音对话 | 3 | ☐ | ☐ | __% |
| 视觉功能 | 3 | ☐ | ☐ | __% |
| 动作表情 | 3 | ☐ | ☐ | __% |
| 灯光控制 | 3 | ☐ | ☐ | __% |
| 联网搜索 | 1 | ☐ | ☐ | __% |
| 系统管理 | 2 | ☐ | ☐ | __% |
| 隐私保护 | 2 | ☐ | ☐ | __% |
| 安全测试 | 2 | ☐ | ☐ | __% |
| 性能测试 | 2 | ☐ | ☐ | __% |
| 压力测试 | 2 | ☐ | ☐ | __% |
| **总计** | **33** | **☐** | **☐** | **__%** |

### 评分

- **功能完整度**: _______ / 10
- **性能表现**: _______ / 10
- **用户体验**: _______ / 10
- **稳定性**: _______ / 10
- **安全性**: _______ / 10

**总体评分**: _______ / 10

### 发布建议

- ☐ **可以发布** - 所有关键功能通过测试
- ☐ **需要修复后发布** - 存在中等严重度问题
- ☐ **不建议发布** - 存在高严重度问题

### 备注

_____________________________________________________
_____________________________________________________
_____________________________________________________
_____________________________________________________

---

**测试完成日期**: _________
**测试人签名**: _________
